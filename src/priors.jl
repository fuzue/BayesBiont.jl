# DEFAULT_PRIORS registry for canonical Kinbiont models.
# Empirical fallback derives priors from `model.guess(data_mat)` for user-defined models.
# Internally we sample in log-space; the registry stores user-facing LogNormal forms.
