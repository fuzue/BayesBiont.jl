# Public entry points: `bayesfit(data, spec[, opts])` and `bayesian_fit` alias.
# Multi-curve dispatches to per-row independent fits in v0.1.
# `group=` kwarg reserved — errors in v0.1, unlocks hierarchical pooling in v0.2.
